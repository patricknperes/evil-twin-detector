import { describe, expect, it } from "vitest";
describe("runtime sync terminology",()=>{it("does not confirm attack",()=>expect("anomalias e níveis de suspeita").not.toContain("Evil Twin confirmado"));});
