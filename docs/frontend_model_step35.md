# Tela Modelo

Fontes:

```http
GET /model
GET /history/models
GET /history/models/{model_version_id}
```

A página serve para status e auditoria científica. Ela não modifica referência,
scaler, modelo ou threshold.
