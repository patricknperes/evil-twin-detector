# Tela Redes observadas

Fonte:

```http
GET /networks
```

Se o backend reiniciar e nenhum novo scan tiver ocorrido:

```text
has_scan = false
```

A interface então orienta o usuário a executar uma nova varredura.

Os detalhes científicos são somente leitura.
